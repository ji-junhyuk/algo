#include <bits/stdc++.h>
using namespace std;

int main()
{
	ios::sync_with_stdio(0);
	cin.tie(0);

	int A, B;
	while (1)
	{
		if (cin.eof())
			break;
		cin >> A >> B;
		cout << A + B << "\n";
		cout << "hello\n";
	}
}
