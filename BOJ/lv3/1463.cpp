#include <bits/stdc++.h>
using namespace std;

int main()
{
	ios::sync_with_stdio(0);
	cin.tie(0);
	
	int N;
	int cnt;

	cnt = 0;
	if (N == 1)
	{
		cout << cnt;
		return ;
	}
	cin >> N;
	while (N != 1)
	{
		if (N == 1)
	}
	cnt = 0;
}
