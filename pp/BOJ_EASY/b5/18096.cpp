#include <iostream>
using namespace std;
typedef long long ll;

/*
 * 번역이 이상하다고 한다.
 * 문제를 풀어보면 {(x+1)*(y+1)-xy-x-y}^n식이 나온다는데, 이 식은 결국 1^n과 같다.
 */

ll N;
int main(void)
{
	ios::sync_with_stdio(false);
	cin.tie(NULL);

	cin >> N;
	cout << 1;
}
