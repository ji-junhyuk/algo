#include <bits/stdc++.h>
using namespace std;

int recur(int n)
{
	int result = 1;

	while (n--)
		result *= 3;
	return result;
}

int cnt[3];
int arr[2187][2187];
int main()
{
	int N;
	int idx;

	ios::sync_with_stdio(0);
	cin.tie(0);
	cin >> N;
	idx

	cout << recur(7);
}
