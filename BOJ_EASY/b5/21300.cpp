#include <iostream>
using namespace std;

int a, b, c, d, e, f;
int main(void)
{
	ios::sync_with_stdio(false);
	cin.tie(NULL);

	cin >> a >> b >> c >> d >> e >> f;
	cout << ((5 * a) + (5 * b) + (5 * c) + (5 * d) + (5 * e) + (5 * f));
}
